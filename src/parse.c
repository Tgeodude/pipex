#include "pipex.h"

void    parsing_file(int argc, char **argv)
{
    char    *s;
    if (argc == 6 && ft_)
    {
    }
    if (argc != 5)
    {
        perror("Incorrect input\n");
        exit(0);
    }

}